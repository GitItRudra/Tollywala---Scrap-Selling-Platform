PORT=4000
JWT_SECRET=replace-this-with-a-long-random-string

# A Postgres connection string. For local development you can point this
# at a local Postgres instance. For a real deployment, use a free
# always-on Postgres from Neon (neon.tech) or Supabase (supabase.com) --
# both have permanent free tiers, unlike Render's 30-day trial Postgres.
# Example: postgres://user:password@host:5432/dbname
DATABASE_URL= postgresql://neondb_owner:npg_r6oHI8sbjqzx@ep-broad-shape-axsi1f8x.c-4.us-east-2.aws.neon.tech/neondb?sslmode=require

# Used only once, to create the very first admin account if none exists yet.
# Change the password after your first login.
ADMIN_EMAIL=admin@tollywala.local
ADMIN_PASSWORD=ChangeMe123!
